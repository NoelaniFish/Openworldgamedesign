
  # Open World Game Design

  This is a code bundle for Open World Game Design/ Butch.world
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
