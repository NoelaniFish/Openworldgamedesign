[build]
  command = "pnpm install && pnpm run build"
  publish = "dist"

[build.environment]
  PNPM_VERSION = "9"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "ALLOWALL"
    Content-Security-Policy = "frame-ancestors *"
